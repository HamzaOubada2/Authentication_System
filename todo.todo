TODO:
    -> Config Database
    -> Create entities
    -> Create dtos(auth)
    -> Config In mail(service)
    -> UserService
    -> AuthService
    -> Guards & decorators(roles,permissions,Jwt_auth)
    -> Interceptors & Exception Filter
    -> main.ts global interceptors
    -> Auth Module & Controller